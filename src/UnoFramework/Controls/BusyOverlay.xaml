<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoFramework.Controls">

    <Style TargetType="local:BusyOverlay">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:BusyOverlay">
                    <Grid>
                        <!-- Main Content -->
                        <ContentPresenter
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />

                        <!-- Busy Overlay: leicht abgedunkelter Backdrop + zentrierte schwarze Card
                             mit weissem Spinner und Text, damit der Indicator auf jedem Untergrund
                             (Split-Layouts, helle/dunkle Themes) eindeutig sichtbar ist - ohne den
                             ganzen Bildschirm zu schwaerzen. -->
                        <Grid x:Name="BusyOverlayPanel"
                              Background="#33000000"
                              Visibility="Collapsed">

                            <Border Background="Black"
                                    CornerRadius="8"
                                    Padding="24,20"
                                    MinWidth="120"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                <StackPanel HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Spacing="12">
                                    <ProgressRing IsActive="True"
                                                  Width="40"
                                                  Height="40"
                                                  Foreground="White" />
                                    <TextBlock Text="{Binding BusyMessage, RelativeSource={RelativeSource TemplatedParent}}"
                                               Foreground="White"
                                               FontSize="13"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"
                                               MaxWidth="280"
                                               Visibility="{Binding BusyMessage, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}, FallbackValue=Collapsed}" />
                                </StackPanel>
                            </Border>
                        </Grid>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="BusyStates">
                                <VisualState x:Name="NotBusy">
                                    <VisualState.Setters>
                                        <Setter Target="BusyOverlayPanel.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Busy">
                                    <VisualState.Setters>
                                        <Setter Target="BusyOverlayPanel.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
